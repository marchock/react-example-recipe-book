import React from 'react';
import styled from 'styled-components';
import FooterStyles from './footer.styles';

const StyledFooter = styled.footer`${FooterStyles}`;

const Footer = () => {
    return (
        <StyledFooter></StyledFooter>
    );
}

export default Footer;
