// Config Keys
const KEYS = {
  API_URL: process.env.API_URL || 'http://localhost:5000/api',
};

export default KEYS;

// TODO: find a way to remove the OR statement. This is a quick fix for running tests.
